  import { Button } from "@mantine/core";
import router from "next/router";
const CreatePayment = () => <div>
  <Button onClick={()=> router.back()}>backPlease</Button>
  CreatePayment</div>;

export default CreatePayment;
